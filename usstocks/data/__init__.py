"""usstocks.data — market data providers (ТЗ §5/§11).

`utex_provider` (Stage D) reuses the existing challenge/manual UTEX feed;
`replay_provider` reads offline CSV fixtures and performs zero network I/O.
"""
