# Simulation dependencies for the virtual LOB market simulator + MT5 shim.
# Install with:  pip install -r requirements_simulation.txt
#
# NOTE: MetaTrader5 is intentionally NOT listed here — in simulation mode it
# is replaced by the virtual shim at simulation/mt5_shim/MetaTrader5/.

sortedcontainers>=2.4.0
pyyaml>=6.0
numpy>=1.24.0
pandas>=2.0.0
