"""Per-table migrations for the ``trade_groups`` table family.

Covers ``trade_groups`` and its companion ``trade_group_actions`` table
(data/trade_group_store.py). The family must always be initialized together;
a partially created family is a broken schema.
"""
