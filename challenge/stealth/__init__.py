"""Anti-fingerprint stealth layer for UTEx/HashHedge browser automation.

Modules:
    humanized_timer          — randomized delays, news/earnings-aware pauses
    humanized_risk_manager   — realtime floating equity tracking, challenge limits
    session_simulator        — ORB session lifecycle (9:30-10:30 ET)
    browser_humanizer        — Bezier mouse paths, stealth fingerprint, hotkeys
    equity_curve_humanizer   — partial exits, trailing SL, early closes
    execution_engine         — StealthExecutionEngine integration layer
"""
